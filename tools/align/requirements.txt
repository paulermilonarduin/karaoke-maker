# Forced alignment toolchain for Karaoke Maker.
# Install inside a dedicated virtualenv (see README.md). ffmpeg must be on PATH.
#
# Versions are pinned to a validated, mutually-compatible set. torch/torchaudio
# are locked on purpose: torchaudio 2.8 dropped its built-in audio save backends,
# so Demucs falls back to soundfile to write the separated vocal stem.

whisperx==3.8.6
demucs==4.0.1
soundfile==0.14.0
torch==2.8.0
torchaudio==2.8.0
